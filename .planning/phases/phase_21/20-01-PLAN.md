---
phase: 21
plan: 01
type: execute
wave: 1
depends_on: [20-01-PLAN.md]
files_modified: [core/db/user_repo.py, core/auth.py, app.py, routes/auth_routes.py]
autonomous: true
requirements: [FR-AUTH-05]
---

<objective>
Consolidate all User SQL logic into 'core/db/user_repo.py' and refactor 'AuthManager' to use the repository. Standardize the user dictionary schema across the application to ensure consistency and maintainability.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Consolidate User SQL into UserRepo</name>
  <files>core/db/user_repo.py</files>
  <action>
    1. Update `UserRepo.get_user_by_id` to return the standardized schema: `{'id', 'email', 'first_name', 'last_name', 'role', 'google_token', 'avatar'}`.
    2. Add `get_user_by_email(email)` to `UserRepo`.
    3. Add `update_password(user_id, hashed_password, version)` to `UserRepo`.
    4. Move `create_user` logic from `AuthManager.register_user` to `UserRepo.create_user`, ensuring it handles password versioning and workspace creation (or move workspace creation to a service later).
  </action>
  <verify>
    Check if `UserRepo` has the new methods and they return the correct schema.
  </verify>
  <done>UserRepo contains all necessary SQL logic for user management.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor AuthManager to use UserRepo</name>
  <files>core/auth.py</files>
  <action>
    1. Update `AuthManager.__init__` to accept or initialize `UserRepo`.
    2. Refactor `verify_user` to use `user_repo.get_user_by_email` and `user_repo.update_password`.
    3. Refactor `register_user` to use `user_repo.create_user`.
    4. Refactor `get_user_by_id` to use `user_repo.get_user_by_id`.
    5. Ensure all returned user dictionaries follow the standardized schema.
  </action>
  <verify>
    Verify `AuthManager` no longer contains raw SQL for user table operations.
  </verify>
  <done>AuthManager is decoupled from raw SQL and uses UserRepo.</done>
</task>

<task type="auto">
  <name>Task 3: Update app.py and routes/auth_routes.py</name>
  <files>app.py, routes/auth_routes.py</files>
  <action>
    1. Update `app.py` user loader to use `auth_manager.get_user_by_id` (which now uses `UserRepo`).
    2. Update `routes/auth_routes.py` to ensure it works with the refactored `AuthManager` and standardized user schema.
  </action>
  <verify>
    Application starts and login/register routes function correctly.
  </verify>
  <done>Routes and app factory are updated to use the consolidated auth logic.</done>
</task>

<task type="auto">
  <name>Task 4: Verification and Regression Testing</name>
  <files>tests/test_auth_integration.py</files>
  <action>
    1. Run existing auth tests if they exist.
    2. Manually verify login and registration if possible (or write a quick script).
  </action>
  <verify>
    All auth-related tests pass.
  </verify>
  <done>Refactor is verified and no regressions found.</done>
</task>

</tasks>

<success_criteria>
1. All User SQL logic is moved to `core/db/user_repo.py`.
2. `AuthManager` uses `UserRepo` for all database interactions.
3. User objects have a consistent schema: `id, email, first_name, last_name, role, google_token, avatar`.
4. Login and Registration flows are fully functional.
</success_criteria>
