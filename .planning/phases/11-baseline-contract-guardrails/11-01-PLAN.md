---
phase: 11-baseline-contract-guardrails
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/11-baseline-contract-guardrails/11-endpoint-manifest.json
  - .planning/phases/11-baseline-contract-guardrails/11-endpoint-snapshot.json
  - scripts/phase11_route_snapshot.py
  - pytest.ini
  - tests/conftest.py
  - tests/contracts/test_contract_routes.py
  - tests/contracts/test_contract_smoke.py
autonomous: true
requirements:
  - TEST-01
  - TEST-03
must_haves:
  truths:
    - "Pytest tooling runs in this repository without ad-hoc manual scripts."
    - "Critical endpoint contract checks exist and are executable on demand."
    - "Baseline endpoint inventory is generated from current routes and can be compared over time."
  artifacts:
    - path: ".planning/phases/11-baseline-contract-guardrails/11-endpoint-manifest.json"
      provides: "Curated source-of-truth list for critical endpoint contracts"
    - path: "scripts/phase11_route_snapshot.py"
      provides: "Route introspection snapshot generator for drift detection"
    - path: "tests/contracts/test_contract_routes.py"
      provides: "Automated method/path/status contract assertions"
    - path: "tests/contracts/test_contract_smoke.py"
      provides: "Critical flow smoke checks for auth/workflow/ai/import-export"
  key_links:
    - from: "scripts/phase11_route_snapshot.py"
      to: ".planning/phases/11-baseline-contract-guardrails/11-endpoint-snapshot.json"
      via: "snapshot writer"
      pattern: "json.dump"
    - from: "tests/contracts/test_contract_routes.py"
      to: ".planning/phases/11-baseline-contract-guardrails/11-endpoint-manifest.json"
      via: "manifest-driven assertions"
      pattern: "json.load"
    - from: "tests/conftest.py"
      to: "app.py"
      via: "create_app fixture"
      pattern: "create_app"
---

<objective>
Establish executable baseline contracts and pytest guardrails for the critical backend surface before any extraction.

Purpose: Lock the current behavior envelope for auth, workflow, AI, and Smart Import/Export so refactor work can be measured against a known baseline.
Output: Endpoint manifest, route snapshot generator, pytest harness, and contract/smoke test suite.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/11-baseline-contract-guardrails/11-CONTEXT.md
@.planning/phases/11-baseline-contract-guardrails/11-RESEARCH.md
@app.py
@core/extensions.py
@core/auth.py
@core/workflow_engine.py
@core/agent_middleware.py
</context>

<threat_model>

## Threat Model

- **T-11-01 (High):** Route contract drift goes undetected before extraction because baseline source is incomplete.
  - Mitigation: Manifest is explicit and includes all D-01 contract groups; snapshot generator compares method/path coverage.
- **T-11-02 (Medium):** Tests become flaky due external integrations (Google/DL service side effects).
  - Mitigation: Use app-factory test client plus controlled fixtures/mocks in contract and smoke tests.
- **T-11-03 (Medium):** Baseline checks pass locally but are not reproducible due missing pytest config.
  - Mitigation: Introduce repository-level pytest.ini and deterministic quick command.
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create critical endpoint manifest and route snapshot generator</name>
  <read_first>
    - app.py
    - .planning/phases/11-baseline-contract-guardrails/11-CONTEXT.md
    - .planning/phases/11-baseline-contract-guardrails/11-RESEARCH.md
  </read_first>
  <files>.planning/phases/11-baseline-contract-guardrails/11-endpoint-manifest.json, scripts/phase11_route_snapshot.py, .planning/phases/11-baseline-contract-guardrails/11-endpoint-snapshot.json</files>
  <behavior>
    - Snapshot command writes current method/path inventory for manifest endpoints.
    - Manifest contains D-01 endpoint groups and D-02 key page routes.
    - Drift is detectable by comparing manifest expected methods to snapshot methods.
  </behavior>
  <action>Create a curated manifest JSON containing critical contract entries for auth, workflow, AI, imports, exports, and key pages (`/`, `/dashboard`, `/imports`, `/exports`). Add a Python script that imports `create_app`, enumerates `app.url_map`, filters to manifest paths, records method/path pairs, and writes `.planning/phases/11-baseline-contract-guardrails/11-endpoint-snapshot.json`. Ensure script output is stable and sorted for deterministic diffs.</action>
  <acceptance_criteria>
    - `.planning/phases/11-baseline-contract-guardrails/11-endpoint-manifest.json` contains `/api/workflows`, `/api/workflow/execute`, `/api/ai/chat`, `/api/imports`, `/api/exports`.
    - `scripts/phase11_route_snapshot.py` contains `create_app` import and writes `11-endpoint-snapshot.json`.
    - Running snapshot command creates JSON entries with both `path` and `methods` keys.
  </acceptance_criteria>
  <verify>
    <automated>python scripts/phase11_route_snapshot.py --out .planning/phases/11-baseline-contract-guardrails/11-endpoint-snapshot.json</automated>
  </verify>
  <done>Manifest and generated snapshot exist and can be re-generated deterministically.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add repository pytest harness and contract tests for critical endpoints</name>
  <read_first>
    - app.py
    - core/extensions.py
    - .planning/phases/11-baseline-contract-guardrails/11-endpoint-manifest.json
    - .planning/phases/11-baseline-contract-guardrails/11-VALIDATION.md
  </read_first>
  <files>pytest.ini, tests/conftest.py, tests/contracts/test_contract_routes.py, tests/contracts/test_contract_smoke.py</files>
  <behavior>
    - `python -m pytest tests/contracts -q` exits 0.
    - Contract tests assert path/method/status and minimal response shape for critical endpoints.
    - Smoke tests cover auth/workflow/ai/import-export baseline behavior without deep payload coupling.
  </behavior>
  <action>Create `pytest.ini` with explicit test discovery for `tests/`. Add `tests/conftest.py` that builds a Flask test client from `create_app` and provides reusable fixtures. Implement `tests/contracts/test_contract_routes.py` to load the manifest and assert method/path presence plus expected status behavior. Implement `tests/contracts/test_contract_smoke.py` for baseline flow checks using minimal top-level keys (`success` or `message`) where responses are JSON.</action>
  <acceptance_criteria>
    - `pytest.ini` exists and includes a `testpaths = tests` setting.
    - `tests/conftest.py` defines an app fixture and a client fixture.
    - `tests/contracts/test_contract_routes.py` loads `11-endpoint-manifest.json`.
    - `tests/contracts/test_contract_smoke.py` contains at least one test each for workflow, AI, and import/export endpoint groups.
    - `python -m pytest tests/contracts -q` returns exit code 0.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/contracts -q</automated>
  </verify>
  <done>Pytest is installed and runnable with a stable contract test baseline for Phase 11 critical flows.</done>
</task>

</tasks>

<verification>
- Run route snapshot generation and ensure output JSON is created.
- Run `python -m pytest tests/contracts -q` and confirm all tests pass.
- Confirm manifest and test files cover all D-01 and D-02 baseline scope decisions.
</verification>

<success_criteria>

- TEST-01 satisfied with a stable pytest harness and quick-run command.
- TEST-03 partially satisfied with critical endpoint contract and smoke coverage baseline.
- Baseline inventory snapshot exists and is reproducible for drift checks.
</success_criteria>

<output>
After completion, create `.planning/phases/11-baseline-contract-guardrails/11-01-SUMMARY.md`.
</output>
