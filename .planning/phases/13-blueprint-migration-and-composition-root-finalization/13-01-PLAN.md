---
phase: 13-blueprint-migration-and-composition-root-finalization
plan: 01
type: execute
wave: 1
depends_on:
  - 12-03
files_modified:
  - app.py
  - routes/__init__.py
  - routes/main_routes.py
  - routes/workflow_routes.py
  - routes/ai_routes.py
  - routes/inventory_routes.py
  - routes/dl_routes.py
  - routes/wallet_routes.py
  - routes/google_routes.py
  - tests/services/test_inventory_route_delegation.py
autonomous: true
requirements:
  - REFAC-03
  - REFAC-04
  - COMP-01
must_haves:
  truths:
    - "Routing ownership is moved out of app.py into dedicated modules by domain."
    - "app.py acts as composition/bootstrap root and does not define app.route handlers."
    - "Critical endpoint path and method contracts remain compatible with baseline manifest."
    - "Application startup and route-map build remain stable after registration refactor."
  artifacts:
    - path: "routes/workflow_routes.py"
      provides: "workflow route ownership"
    - path: "routes/ai_routes.py"
      provides: "ai route ownership"
    - path: "routes/inventory_routes.py"
      provides: "inventory route ownership"
    - path: "routes/dl_routes.py"
      provides: "dl proxy and product-sales-history route ownership"
    - path: "routes/wallet_routes.py"
      provides: "wallet and subscription route ownership"
    - path: "routes/google_routes.py"
      provides: "google oauth and google files route ownership"
    - path: "routes/main_routes.py"
      provides: "migration bridge and delegated registration"
    - path: "app.py"
      provides: "composition root registration only"
  key_links:
    - from: "app.py"
      to: "routes/*"
      via: "create_app registration order"
      pattern: "register_blueprint|register_main_routes"
    - from: "tests/contracts/test_contract_routes.py"
      to: "11-endpoint-manifest.json"
      via: "route registry parity"
      pattern: "manifest"
    - from: "scripts/phase11_guardrail_check.py"
      to: "app route map"
      via: "snapshot plus contract gate"
      pattern: "Guardrail"
---

<objective>
Migrate route ownership from app.py to dedicated domain modules and finalize app.py as composition/bootstrap root while preserving endpoint compatibility.

Purpose: Satisfy Phase 13 routing decomposition and composition-root requirements without breaking baseline contract guardrails.
Output: Domain route modules registered via app factory, plus parity evidence from tests and guardrails.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-blueprint-migration-and-composition-root-finalization/13-CONTEXT.md
@.planning/phases/11-baseline-contract-guardrails/11-endpoint-manifest.json
@app.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Domain route module extraction and composition registration</name>
  <files>app.py, routes/__init__.py, routes/workflow_routes.py, routes/ai_routes.py, routes/inventory_routes.py, routes/dl_routes.py</files>
  <behavior>
    - Workflow, AI, inventory, and DL routes are no longer declared with app.route in app.py.
    - create_app registers extracted route modules in deterministic order.
  </behavior>
  <action>Extract route groups from app.py into domain modules under routes/. Register auth, inventory, workflow, ai, and dl modules in create_app. Keep route paths/methods unchanged and maintain route-level authentication/CSRF behavior.</action>
  <verify>
    <automated>python -m pytest tests/services/test_inventory_route_delegation.py tests/services/test_workflow_service.py tests/services/test_ai_chat_service.py -q</automated>
  </verify>
  <done>Core route domains are modularized and app startup still resolves route map correctly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Main route bridge split and endpoint-name preservation</name>
  <files>routes/main_routes.py, routes/wallet_routes.py, routes/google_routes.py, app.py</files>
  <behavior>
    - app.py remains a composition root with no app.route handlers.
    - Legacy endpoint names used by templates remain valid (no main.* prefix regressions).
  </behavior>
  <action>Move remaining route surface into routes/main_routes.py with register_main_routes(app). Split wallet/subscription and google oauth/files routes into dedicated registrars and delegate from main_routes. Ensure registration style keeps existing endpoint names unchanged for template url_for calls.</action>
  <verify>
    <automated>python -m pytest tests/contracts/test_contract_smoke.py -q</automated>
  </verify>
  <done>Main route bridge is decomposed and endpoint name compatibility is preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Contract parity and guardrail evidence closeout</name>
  <files>tests/services/test_inventory_route_delegation.py, .planning/phases/11-baseline-contract-guardrails/11-endpoint-snapshot.json, .planning/phases/11-baseline-contract-guardrails/11-guardrail-report.md</files>
  <behavior>
    - Baseline endpoint path/method parity remains intact after extraction.
    - Guardrail gate passes with updated snapshot evidence.
  </behavior>
  <action>Update route-delegation tests to target extracted modules directly where needed. Re-run contract and guardrail commands to produce parity evidence and ensure no baseline drift.</action>
  <verify>
    <automated>python -m pytest tests/contracts/test_contract_routes.py tests/contracts/test_contract_smoke.py -q && python scripts/phase11_route_snapshot.py && python scripts/phase11_guardrail_check.py</automated>
  </verify>
  <done>Phase-level parity gate passes and baseline compatibility is confirmed.</done>
</task>

</tasks>

<verification>
- Route map builds successfully from app import.
- Service/delegation tests pass for extracted domains.
- Manifest contract tests pass.
- Phase11 guardrail check passes.
</verification>

<success_criteria>

- REFAC-03 satisfied by composition-root app.py with no app.route handlers.
- REFAC-04 satisfied by dedicated route modules owning domain routes.
- COMP-01 satisfied by unchanged critical endpoint path/method contracts.
</success_criteria>

<output>
After completion, create .planning/phases/13-blueprint-migration-and-composition-root-finalization/13-01-SUMMARY.md and 13-01-VERIFICATION.md.
</output>
