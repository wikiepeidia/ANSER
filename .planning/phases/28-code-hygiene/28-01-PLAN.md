---
phase: 28-code-hygiene
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - core/services/analytics_service.py
  - core/google_integration.py
  - core/utils.py
  - tests/test_code_hygiene.py
autonomous: true
requirements:
  - HYG-01
  - HYG-02
  - HYG-03
---

<objective>
Close the remaining code-quality bugs in analytics_service.py, google_integration.py, and utils.py that were deferred from earlier phases: replace any hardcoded GA property ID usage with Config, remove duplicate except blocks, escape single quotes in Drive API query strings, and migrate format_workspace_tree to name-based row field access.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Fix analytics_service.py hygiene issues</name>
  <files>core/services/analytics_service.py</files>
  <action>
  Ensure AnalyticsService uses get_logger(__name__), reads GA_PROPERTY_ID exclusively from Config (no hardcoded fallback), and has no duplicate except clause. Remove any print() calls.
  </action>
  <acceptance_criteria>
  - `from ..logger import get_logger` and `logger = get_logger(__name__)` present at module level.
  - No `print(` calls in analytics_service.py.
  - `AnalyticsService.__init__` sets `self.property_id = Config.GA_PROPERTY_ID`.
  - Source contains at most one `except Exception` per logical block; no duplicate bare-except.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Fix google_integration.py single-quote escaping in list_files</name>
  <files>core/google_integration.py</files>
  <action>
  Add or verify _escape_drive_query_value helper that replaces backslashes then single quotes. Apply it to query_text in list_files before embedding in the Drive API q= string.
  </action>
  <acceptance_criteria>
  - `_escape_drive_query_value` exists in core/google_integration.py.
  - list_files applies it to query_text before building q_parts.
  - Query string for "Bob's invoice" contains `name contains 'Bob\'s invoice'`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Fix utils.py format_workspace_tree tuple indexing</name>
  <files>core/utils.py</files>
  <action>
  Introduce _workspace_mapping helper that converts sqlite3.Row, plain tuples, or dicts to a dict keyed by column name. Update format_workspace_tree to call _workspace_mapping on each workspace before accessing fields.
  </action>
  <acceptance_criteria>
  - `_workspace_mapping` present in core/utils.py.
  - `format_workspace_tree` uses `row['type']`, `row['id']`, `row['name']`, `row['description']` — no integer subscript access (workspace[0], workspace[2], etc).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Add focused hygiene regression tests</name>
  <files>tests/test_code_hygiene.py</files>
  <action>
  Write pytest tests for each of the three hygiene fixes: Config property ID usage, Drive query escaping, and named-field workspace row access.
  </action>
  <acceptance_criteria>
  - `python -m pytest tests/test_code_hygiene.py -v` reports all tests passed.
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. `python -m pytest tests/test_code_hygiene.py -v`
2. `python -m compileall core/services/analytics_service.py core/google_integration.py core/utils.py`
3. `python -c "from core.services.analytics_service import AnalyticsService; print('analytics import ok')"`
</verification>

<success_criteria>
- HYG-01: analytics_service.py uses get_logger(), no duplicate except, reads GA_PROPERTY_ID from Config.
- HYG-02: list_files escapes single quotes in Drive API query strings.
- HYG-03: format_workspace_tree accesses row fields by name, not tuple index.
</success_criteria>
