---
phase: 24-async-task-infrastructure
plan: 02
type: execute
wave: 2
depends_on: [24-01]
files_modified: [routes/ai_routes.py, core/services/ai_chat_service.py]
autonomous: true
requirements: [NFR-PERF-05]
must_haves:
  truths:
    - "AI Chat requests return a job_id and status 'processing'"
    - "AI Chat status endpoint returns results from Redis/RQ"
    - "AI Chat processing occurs in the background worker process"
  artifacts:
    - path: "core/services/ai_chat_service.py"
      contains: "background_ai_job"
---

<objective>
Refactor the AI Chat system to use the RQ background queue instead of raw threading. This ensures job persistence and allows for scaling the AI processing independently from the web server.

Purpose: Improve stability and observability of AI chat jobs.
Output: Refactored AI routes and services using Redis-backed job management.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-async-task-infrastructure/24-01-SUMMARY.md
@routes/ai_routes.py
@core/services/ai_chat_service.py
@core/config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move AI Background Task to Service Layer</name>
  <files>core/services/ai_chat_service.py, routes/ai_routes.py</files>
  <action>
    1. Move the logic of `background_ai_task` from `routes/ai_routes.py` to `core/services/ai_chat_service.py` as a new function `background_ai_job(user_id, message, job_id)`.
    2. Refactor `background_ai_job` to use Redis (via `redis.from_url(Config.REDIS_URL)`) to store job status and results instead of `save_job_file`.
    3. Use a standardized key pattern in Redis, e.g., `job:{job_id}`.
    4. Ensure all necessary imports (Database, AgentMiddleware, requests, etc.) are available in `ai_chat_service.py`.
  </action>
  <verify>
    <automated>python -c "from core.services.ai_chat_service import background_ai_job; print('background_ai_job is available')"</automated>
  </verify>
  <done>AI background logic is moved to the service layer and uses Redis for persistence.</done>
</task>

<task type="auto">
  <name>Task 2: Update AI Routes to Enqueue Jobs</name>
  <files>routes/ai_routes.py</files>
  <action>
    1. In `ai_chat` route: Replace `threading.Thread(...).start()` with RQ enqueuing.
    2. Import `Redis` and `Queue` from `rq`.
    3. Initialize the queue using `current_app.config['REDIS_URL']`.
    4. Enqueue `background_ai_job` and pass the `user_id`, `message`, and a generated `job_id`.
    5. Return the `job_id` to the client.
  </action>
  <verify>
    <automated>grep "q.enqueue" routes/ai_routes.py</automated>
  </verify>
  <done>AI Chat route now enqueues tasks to Redis/RQ.</done>
</task>

<task type="auto">
  <name>Task 3: Refactor Job Status Polling</name>
  <files>routes/ai_routes.py, core/services/ai_chat_service.py</files>
  <action>
    1. Update `ai_job_status(job_id)` route to fetch the job state from Redis.
    2. If using RQ's built-in job tracking, use `Job.fetch(job_id, connection=redis_conn)`.
    3. Map RQ job status (queued, started, finished, failed) to the application's status vocabulary (pending, processing, completed, failed).
    4. Remove the legacy `save_job_file` and `load_job_file` functions and delete the `jobs/` directory logic.
  </action>
  <verify>
    <automated>grep -v "load_job_file" routes/ai_routes.py</automated>
  </verify>
  <done>Status polling now uses Redis/RQ as the source of truth.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> Status API | Users can poll status for any `job_id`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-03 | Information Disclosure | AI Status API | mitigate | In `ai_job_status`, verify that the job belongs to the `current_user` by storing the `user_id` in the Redis job metadata and checking it before returning results. |
</threat_model>

<verification>
Manual verification: 
1. Start Redis.
2. Start `worker.py`.
3. Start `app.py`.
4. Send a chat message via Postman or UI.
5. Poll the status endpoint using the returned `job_id`.
</verification>

<success_criteria>
- AI chat jobs are processed by `worker.py`.
- No new files created in `jobs/` directory.
- `app.py` process is no longer responsible for running AI calls.
</success_criteria>

<output>
Create `.planning/phases/24-async-task-infrastructure/24-02-SUMMARY.md` when done
</output>
