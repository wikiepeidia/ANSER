---
phase: 24-async-task-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [requirements-base.txt, core/config.py, worker.py, .env.example]
autonomous: true
requirements: [NFR-PERF-05]
must_haves:
  truths:
    - "Redis connectivity can be verified via a CLI command or script"
    - "RQ worker can be started and listens to the 'default' queue"
    - "Project dependencies include redis and rq"
  artifacts:
    - path: "worker.py"
      provides: "RQ worker entry point"
    - path: "core/config.py"
      contains: "REDIS_URL"
---

<objective>
Setup the foundational infrastructure for asynchronous task management using Redis and RQ. This replaces the current threading-based approach with a robust, persistent queue system.

Purpose: Provide a reliable way to handle long-running AI and OCR tasks without blocking the web server or losing tasks on restart.
Output: Updated dependencies, Redis configuration, and a functional background worker script.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@requirements-base.txt
@core/config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Redis/RQ Dependencies and Config</name>
  <files>requirements-base.txt, core/config.py, .env.example</files>
  <action>
    1. Add `redis>=5.0.0` and `rq>=1.16.0` to `requirements-base.txt` (per BE-07).
    2. Add `REDIS_URL = os.environ.get('REDIS_URL', 'redis://localhost:6379/0')` to `core/config.py` in the `Config` class.
    3. Add `REDIS_URL=redis://localhost:6379/0` to `.env.example`.
  </action>
  <verify>
    <automated>grep -E "redis|rq" requirements-base.txt && grep "REDIS_URL" core/config.py</automated>
  </verify>
  <done>Dependencies added and configuration key established.</done>
</task>

<task type="auto">
  <name>Task 2: Create Background Worker Script</name>
  <files>worker.py</files>
  <action>
    Create `worker.py` in the project root. It should:
    1. Import `redis`, `rq.Worker`, `rq.Queue`, and `rq.Connection`.
    2. Load `REDIS_URL` from environment or use default `redis://localhost:6379/0`.
    3. Define a connection to Redis.
    4. Provide a main block that starts an RQ Worker listening on `default` and `high` queues.
    5. Ensure `PYTHONPATH` includes the current directory so it can import from `routes` or `core`.
  </action>
  <verify>
    <automated>python -c "import worker; print('Worker module importable')"</automated>
  </verify>
  <done>worker.py exists and is importable.</done>
</task>

<task type="auto">
  <name>Task 3: Redis Connectivity Health Check</name>
  <files>scripts/check_redis.py</files>
  <action>
    Create a small utility script `scripts/check_redis.py` to verify that the app can talk to a Redis instance using the configured URL.
  </action>
  <verify>
    <automated>python scripts/check_redis.py || echo "Redis not running - expected if no local instance, but script must exist"</automated>
  </verify>
  <done>Health check script created and verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App -> Redis | Internal communication; typically trusted but requires auth in production. |
| Worker -> APIs | Worker calls AI/DL APIs; transit should be encrypted (HTTPS). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | Information Disclosure | Redis | mitigate | Ensure Redis is protected by a password (REDIS_URL auth) and not exposed to the public internet. |
| T-24-02 | Tampering | Redis | mitigate | Use specific Redis DB indices or prefixes for the queue to avoid conflict with other services. |
| T-24-SC | Tampering | npm/pip installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
Verify that `pip install -r requirements-base.txt` succeeds and `worker.py` can be launched (even if it fails to connect to Redis initially).
</verification>

<success_criteria>
- Project has Redis/RQ dependencies.
- `worker.py` is ready for deployment.
- Configuration for Redis is centralized in `core/config.py`.
</success_criteria>

<output>
Create `.planning/phases/24-async-task-infrastructure/24-01-SUMMARY.md` when done
</output>
