---
phase: 32-error-handling-data-access-cleanup
plan: 01
type: execute
requirements:
  - SEC-04
  - PLAT-01
  - PLAT-02
---

# Phase 32 Plan 01: Error Handling & Data Access Cleanup

## Tasks

1. Add `safe_api_error()` correlation responses for API 500s.
2. Apply safe 500 handling to `app.py`, `sales_routes.py`, and `ai_routes.py`.
3. Replace `routes/sales_routes.py` global `db_manager` access with `current_app.extensions['database']`.
4. Move Google OAuth raw SQL to repository/facade methods.
5. Preserve HTTP exception behavior for non-API routes.

## Verification

- `pytest -q`
- Static scan for named raw SQL/hash/CSRF patterns.

