---
phase: 15-parity-verification-across-security-data-and-async-flows
plan: 02
type: execute
wave: 2
depends_on:
  - 15-01
files_modified:
  - tests/parity/test_data_async_parity.py
autonomous: true
requirements:
  - DATA-01
  - DATA-02
must_haves:
  truths:
    - "Critical DB write paths are validated for both SQLite and PostgreSQL compatibility modes."
    - "Async AI lifecycle states pending/processing/completed/failed are covered by deterministic tests."
    - "Parity checks do not depend on external network or live Postgres availability."
  artifacts:
    - path: "tests/parity/test_data_async_parity.py"
      provides: "database-mode write parity and async lifecycle parity tests"
  key_links:
    - from: "tests/parity/test_data_async_parity.py"
      to: "core/database.py"
      via: "PGShim and write-path compatibility assertions"
      pattern: "PGShim|create_workflow|add_ai_message"
    - from: "tests/parity/test_data_async_parity.py"
      to: "routes/ai_routes.py"
      via: "background task lifecycle state assertions"
      pattern: "pending|processing|completed|failed"
---

<objective>
Validate data and async lifecycle parity through deterministic automated tests for DB write compatibility and AI job state transitions.

Purpose: Close DATA-01 and DATA-02 with executable parity evidence independent of external infra.
Output: Parity test module and passing test run.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-parity-verification-across-security-data-and-async-flows/15-CONTEXT.md
@core/database.py
@core/services/ai_chat_service.py
@routes/ai_routes.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add DB write-path parity tests for SQLite and PostgreSQL modes</name>
  <files>tests/parity/test_data_async_parity.py</files>
  <behavior>
    - SQLite-mode write method commits and preserves placeholder behavior.
    - PostgreSQL compatibility path translates placeholders and preserves write intent.
  </behavior>
  <action>Create tests using fakes/mocks around Database + PGShim to assert write-path behavior for representative critical writes in both modes without live DB dependencies.</action>
  <verify>
    <automated>python -m pytest tests/parity/test_data_async_parity.py::test_db_write_parity_sqlite_mode tests/parity/test_data_async_parity.py::test_db_write_parity_postgres_shim_mode -q</automated>
  </verify>
  <done>DATA-01 parity checks pass for both DB modes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add async AI lifecycle parity tests</name>
  <files>tests/parity/test_data_async_parity.py</files>
  <behavior>
    - Job creation records pending and returns processing state metadata.
    - Background task success path records processing then completed.
    - Background task failure path records processing then failed.
  </behavior>
  <action>Add tests for ai_chat_service.create_chat_job and routes.ai_routes.background_ai_task using deterministic monkeypatching to assert lifecycle transitions pending/processing/completed/failed.</action>
  <verify>
    <automated>python -m pytest tests/parity/test_data_async_parity.py::test_async_job_pending_and_processing_parity tests/parity/test_data_async_parity.py::test_background_ai_task_completed_parity tests/parity/test_data_async_parity.py::test_background_ai_task_failed_parity -q</automated>
  </verify>
  <done>DATA-02 lifecycle states are explicitly validated and passing.</done>
</task>

<task type="auto">
  <name>Task 3: Execute full data and async parity suite</name>
  <files>tests/parity/test_data_async_parity.py</files>
  <action>Run complete parity module and ensure all DB-mode and async lifecycle checks pass.</action>
  <verify>
    <automated>python -m pytest tests/parity/test_data_async_parity.py -q</automated>
  </verify>
  <done>DATA-01 and DATA-02 parity checks are green and reproducible.</done>
</task>

</tasks>

<verification>
- DB write-path parity tests pass for SQLite and PostgreSQL-shim modes.
- Async AI lifecycle parity tests pass across required states.
</verification>

<success_criteria>

- DATA-01 and DATA-02 are covered by deterministic automated tests and passing outputs.
</success_criteria>

<output>
After completion, create .planning/phases/15-parity-verification-across-security-data-and-async-flows/15-02-SUMMARY.md and 15-VERIFICATION.md.
</output>
