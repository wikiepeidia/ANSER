---
phase: 15-parity-verification-across-security-data-and-async-flows
plan: 01
type: execute
wave: 1
depends_on:
  - 14-02
files_modified:
  - tests/parity/test_endpoint_middleware_parity.py
autonomous: true
requirements:
  - COMP-02
  - SEC-01
must_haves:
  truths:
    - "Critical protected endpoints preserve expected unauthorized status and minimal payload shape."
    - "API unauthorized behavior remains JSON 401 while protected pages still redirect to signin."
    - "CSRF failure on protected API POST returns deterministic JSON error response."
    - "Rate-limit runtime baseline behavior is explicitly verified for current backend profile."
  artifacts:
    - path: "tests/parity/test_endpoint_middleware_parity.py"
      provides: "contract payload and middleware parity assertions"
  key_links:
    - from: "tests/parity/test_endpoint_middleware_parity.py"
      to: "routes/workflow_routes.py"
      via: "unauthorized and csrf route checks"
      pattern: "/api/workflows|Unauthorized|CSRF"
    - from: "tests/parity/test_endpoint_middleware_parity.py"
      to: "app.py"
      via: "unauthorized handler and config parity"
      pattern: "login_unauthorized|RATELIMIT_ENABLED"
---

<objective>
Validate endpoint status/payload compatibility and middleware parity for auth, CSRF, and rate-limit expectations after routing refactor.

Purpose: Close COMP-02 and SEC-01 with executable checks tied to current backend behavior.
Output: Parity test module and passing verification output.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-parity-verification-across-security-data-and-async-flows/15-CONTEXT.md
@tests/conftest.py
@tests/contracts/test_contract_smoke.py
@routes/workflow_routes.py
@app.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add critical endpoint status and payload parity checks</name>
  <files>tests/parity/test_endpoint_middleware_parity.py</files>
  <behavior>
    - Protected API endpoints return 401 JSON with stable error shape for unauthenticated requests.
    - Protected page endpoints continue redirect behavior for unauthenticated requests.
  </behavior>
  <action>Create parity tests for representative endpoints from workflow/import-export/page groups, asserting status code and minimal payload keys/redirect location compatibility.</action>
  <verify>
    <automated>python -m pytest tests/parity/test_endpoint_middleware_parity.py::test_unauthorized_api_returns_json_401 tests/parity/test_endpoint_middleware_parity.py::test_unauthorized_page_redirects_to_signin -q</automated>
  </verify>
  <done>Status and payload parity checks pass for selected critical routes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add CSRF and rate-limit baseline parity checks</name>
  <files>tests/parity/test_endpoint_middleware_parity.py</files>
  <behavior>
    - Missing CSRF token on protected API POST returns JSON 400 with expected message.
    - Current rate-limit baseline behavior remains explicit and verifiable.
  </behavior>
  <action>Add tests asserting CSRF failure payload on /api/workflows POST with CSRF enabled and baseline rate-limit config/behavior expectations for current backend profile.</action>
  <verify>
    <automated>python -m pytest tests/parity/test_endpoint_middleware_parity.py::test_api_csrf_failure_returns_json_400 tests/parity/test_endpoint_middleware_parity.py::test_rate_limit_profile_parity -q</automated>
  </verify>
  <done>Middleware parity checks for CSRF and rate-limit baseline pass.</done>
</task>

<task type="auto">
  <name>Task 3: Execute full endpoint and middleware parity suite</name>
  <files>tests/parity/test_endpoint_middleware_parity.py</files>
  <action>Run the entire parity test module and ensure results are green without affecting existing contract/service suites.</action>
  <verify>
    <automated>python -m pytest tests/parity/test_endpoint_middleware_parity.py -q</automated>
  </verify>
  <done>COMP-02 and SEC-01 parity checks are executable and passing.</done>
</task>

</tasks>

<verification>
- Endpoint status/payload parity assertions pass.
- Middleware parity assertions (auth/CSRF/rate-limit baseline) pass.
</verification>

<success_criteria>

- COMP-02 and SEC-01 are covered by automated parity tests and passing outputs.
</success_criteria>

<output>
After completion, create .planning/phases/15-parity-verification-across-security-data-and-async-flows/15-01-SUMMARY.md
</output>
