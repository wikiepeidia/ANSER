---
phase: 22-dependency-isolation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [requirements-base.txt, requirements-ml.txt, requirements-dev.txt, requirements.txt, README.md]
autonomous: true
requirements: [NFR-INF-01]

must_haves:
  truths:
    - "New requirements files exist in the root directory"
    - "requirements-base.txt contains only non-ML core dependencies"
    - "requirements-ml.txt contains torch, tensorflow, paddlepaddle, etc."
    - "README.md explains the new dependency structure"
  artifacts:
    - path: "requirements-base.txt"
      provides: "Core application dependencies"
    - path: "requirements-ml.txt"
      provides: "Machine Learning / Deep Learning dependencies"
    - path: "requirements-dev.txt"
      provides: "Development and testing dependencies"
---

<objective>
Refactor the project's dependency management by splitting the bloated requirements into modular files.
Purpose: Reduce the environment size for the main web application and prepare ML components for isolation.
Output: Three new requirements files (base, ml, dev) and updated documentation.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@package/requirements.txt
@ai_agent_service/requirements.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate requirements-base.txt and requirements-ml.txt</name>
  <files>requirements-base.txt, requirements-ml.txt</files>
  <action>
    Extract dependencies from package/requirements.txt and categorize them.
    - requirements-base.txt: Flask, Flask-*, Werkzeug, requests, authlib, google-*, pandas, openpyxl, SQLAlchemy, alembic, psycopg*, passlib, python-dotenv, gdown, pytz, lunardate.
    - requirements-ml.txt: torch, tensorflow, transformers, accelerate, bitsandbytes, protobuf, sentencepiece, sentence-transformers, timm, einops, chromadb, ultralytics, easyocr, paddlepaddle, paddleocr, opencv-python-headless, Pillow, pytesseract, scikit-learn, pypdf, python-docx, ddgs, addict, easydict.
    Ensure versions are preserved from package/requirements.txt.
  </action>
  <verify>
    <automated>ls requirements-base.txt && ls requirements-ml.txt</automated>
  </verify>
  <done>Base and ML requirement files are created with correct package distribution.</done>
</task>

<task type="auto">
  <name>Task 2: Update requirements-dev.txt and root requirements.txt</name>
  <files>requirements-dev.txt, requirements.txt</files>
  <action>
    - Ensure requirements-dev.txt includes pytest, pytest-cov, pytest-mock, pytest-timeout, and add 'ruff' as requested in TODO.md.
    - Create a root requirements.txt that contains: '-r requirements-base.txt'. This maintains compatibility with standard tooling while keeping the default install small.
  </action>
  <verify>
    <automated>grep ruff requirements-dev.txt && grep "\-r requirements-base.txt" requirements.txt</automated>
  </verify>
  <done>Development dependencies updated and root requirements.txt redirects to base.</done>
</task>

<task type="auto">
  <name>Task 3: Update README.md with dependency instructions</name>
  <files>README.md</files>
  <action>
    Add a section "Installation & Dependencies" to README.md.
    Explain:
    - Standard install: `pip install -r requirements.txt` (Installs base app)
    - Full ML install (if running services locally): `pip install -r requirements-ml.txt`
    - Dev install: `pip install -r requirements-dev.txt`
    Mention that dl_service is being moved to a separate repository/environment.
  </action>
  <verify>
    <automated>grep "Installation & Dependencies" README.md</automated>
  </verify>
  <done>Documentation updated with clear instructions for different dependency tiers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Dependency Source | External packages from PyPI |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-SC | Tampering | npm/pip installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
Verify that 'pip install -r requirements-base.txt' produces a working environment for app.py (mocking/skipping DL imports if necessary).
</verification>

<success_criteria>
Modular requirements files are present and correctly categorized, significantly reducing the default environment size.
</success_criteria>

<output>
Create .planning/phases/22-dependency-isolation/22-01-SUMMARY.md when done
</output>
