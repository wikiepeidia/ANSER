---
phase: 12-service-boundary-extraction
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - app.py
  - core/services/workflow_service.py
  - core/services/ai_chat_service.py
  - tests/services/test_workflow_service.py
  - tests/services/test_ai_chat_service.py
  - .planning/phases/12-service-boundary-extraction/12-handler-extraction-map.md
autonomous: true
requirements:
  - REFAC-05
  - TEST-02
must_haves:
  truths:
    - "Workflow and AI business branches execute through service functions, not route-local business logic blocks."
    - "Service-level tests exist for workflow and AI extraction behavior."
    - "Extraction map status reflects delegated workflow/AI handlers with test references."
  artifacts:
    - path: "core/services/workflow_service.py"
      provides: "Workflow business logic extraction target"
    - path: "core/services/ai_chat_service.py"
      provides: "AI chat lifecycle extraction target"
    - path: "tests/services/test_workflow_service.py"
      provides: "Workflow service unit coverage"
    - path: "tests/services/test_ai_chat_service.py"
      provides: "AI service unit coverage"
  key_links:
    - from: "app.py"
      to: "core/services/workflow_service.py"
      via: "run_workflow and workflow CRUD route delegation"
      pattern: "workflow_service\."
    - from: "app.py"
      to: "core/services/ai_chat_service.py"
      via: "ai_chat/history/status delegation"
      pattern: "ai_chat_service\."
    - from: ".planning/phases/12-service-boundary-extraction/12-handler-extraction-map.md"
      to: "tests/services/test_workflow_service.py"
      via: "test reference column updates"
      pattern: "test_workflow_service"
---

<objective>
Extract workflow and AI route business logic into dedicated service modules while preserving endpoint contracts.

Purpose: Advance REFAC-05 by delegating high-risk domains first and enforce TEST-02 through direct service tests.
Output: Delegated workflow/AI routes, service implementations, and unit tests with map status updates.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-service-boundary-extraction/12-CONTEXT.md
@.planning/phases/12-service-boundary-extraction/12-RESEARCH.md
@.planning/phases/12-service-boundary-extraction/12-VALIDATION.md
@.planning/phases/12-service-boundary-extraction/12-handler-extraction-map.md
@app.py
@core/workflow_engine.py
@core/agent_middleware.py
@core/database.py
@scripts/phase11_guardrail_check.py
</context>

<threat_model>

## Threat Model

- **T-12-04 (High):** Delegation refactor changes route behavior (status/payload) while moving workflow/AI logic.
  - Mitigation: Keep HTTP response shaping in `app.py` and verify parity with guardrail command after delegation.
- **T-12-05 (Medium):** Workflow/AI services keep hidden Flask dependencies and break TEST-02 intent.
  - Mitigation: Route services consume plain inputs and are tested directly in `tests/services`.
- **T-12-06 (Medium):** Extraction map is not updated, breaking REFAC-06 traceability.
  - Mitigation: Mandatory map status updates to `delegated` and `verified` with explicit test file references.
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Delegate workflow execution and workflow CRUD logic to workflow_service</name>
  <read_first>
    - app.py
    - core/services/workflow_service.py
    - core/workflow_engine.py
    - core/database.py
    - .planning/phases/12-service-boundary-extraction/12-handler-extraction-map.md
  </read_first>
  <files>core/services/workflow_service.py, app.py, tests/services/test_workflow_service.py</files>
  <behavior>
    - `run_workflow` route delegates token parsing and execution call into workflow_service.
    - Workflow CRUD routes delegate DB/query serialization logic into workflow_service functions.
    - Workflow service tests validate expected return shapes and error behavior.
  </behavior>
  <action>Implement in `workflow_service.py` concrete functions `execute_user_workflow(workflow_data, google_token_raw)`, `list_workflows_for_user(db_conn, user_id)`, `save_workflow_for_user(db_conn, user_id, payload)`, `delete_workflow_for_user(db_conn, user_id, workflow_id)`, and `get_workflow_for_user(db_conn, user_id, workflow_id)`. Move business logic blocks from `run_workflow`, `get_user_workflows`, `save_workflow`, `delete_workflow`, and `get_single_workflow` into these functions. Update `app.py` routes so they only parse request payload, call service functions, and return HTTP JSON/status codes. Add `tests/services/test_workflow_service.py` to cover token parsing fallback, workflow list serialization, and create/update branch behavior.</action>
  <acceptance_criteria>
    - `app.py` workflow routes call `workflow_service.` functions instead of embedding DB execution logic inline.
    - `workflow_service.py` contains all five required function definitions.
    - `tests/services/test_workflow_service.py` exists and includes tests for token parsing and save-workflow create/update behavior.
    - `python -m pytest tests/services/test_workflow_service.py -q` exits with code 0.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/services/test_workflow_service.py -q</automated>
  </verify>
  <done>Workflow route business logic is service-owned and workflow service tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Delegate AI chat lifecycle logic to ai_chat_service and verify parity</name>
  <read_first>
    - app.py
    - core/services/ai_chat_service.py
    - core/agent_middleware.py
    - core/database.py
    - scripts/phase11_guardrail_check.py
  </read_first>
  <files>core/services/ai_chat_service.py, app.py, tests/services/test_ai_chat_service.py, .planning/phases/12-service-boundary-extraction/12-handler-extraction-map.md</files>
  <behavior>
    - `ai_chat`, `get_chat_history`, and `clear_chat_history` routes delegate their business branches to ai_chat_service.
    - AI service tests cover greeting shortcut flow, empty-message validation, and history formatting behavior.
    - Extraction map updates workflow/AI rows to delegated/verified status with test links.
  </behavior>
  <action>Implement in `ai_chat_service.py` functions `normalize_message(raw_message)`, `resolve_greeting_reply(message)`, `create_chat_job(user_id, message, save_job_file_fn)`, `fetch_chat_history(db_conn, user_id, limit=50)`, and `clear_chat_history_rows(db_conn, user_id)`. Refactor `app.py` to use these functions inside `ai_chat`, `get_chat_history`, and `clear_chat_history` while keeping existing endpoint paths and status semantics intact. Add `tests/services/test_ai_chat_service.py` covering greeting detection matrix (`xin chào`, `hello`, `hi`, `chào`), empty-message rejection, and history list formatting. Update extraction-map rows for `ai_chat`, `get_chat_history`, and `clear_chat_history` to `delegated` and add test reference paths.</action>
  <acceptance_criteria>
    - `app.py` AI routes call `ai_chat_service.` functions for message normalization/history operations.
    - `tests/services/test_ai_chat_service.py` includes greeting and empty-message test cases.
    - Extraction map contains updated statuses (`delegated` or `verified`) for AI rows with `tests/services/test_ai_chat_service.py` references.
    - `python -m pytest tests/services/test_ai_chat_service.py -q` exits with code 0.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/services/test_ai_chat_service.py -q</automated>
  </verify>
  <done>AI chat route business logic is delegated and service-level tests pass with traceability updates.</done>
</task>

</tasks>

<verification>
- Run workflow and AI service unit tests.
- Confirm extraction map statuses and test links are updated for workflow/AI handlers.
- Run guardrail parity gate to detect accidental endpoint contract drift.
</verification>

<success_criteria>

- REFAC-05 progress is visible in workflow and AI domains through service delegation.
- TEST-02 is extended with dedicated workflow and AI service tests.
- Phase 11 guardrail command remains a valid parity checkpoint after delegation.
</success_criteria>

<output>
After completion, create `.planning/phases/12-service-boundary-extraction/12-02-SUMMARY.md`.
</output>
