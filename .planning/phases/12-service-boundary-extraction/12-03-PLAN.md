---
phase: 12-service-boundary-extraction
plan: 03
type: execute
wave: 3
depends_on:
  - 12-02
files_modified:
  - app.py
  - core/services/inventory_tx_service.py
  - tests/services/test_inventory_tx_service.py
  - tests/services/test_inventory_route_delegation.py
  - .planning/phases/12-service-boundary-extraction/12-handler-extraction-map.md
autonomous: true
requirements:
  - REFAC-05
  - REFAC-06
  - TEST-02
must_haves:
  truths:
    - "Import/export inventory transaction mutations run through a dedicated service module."
    - "Service-level tests cover transaction success paths and stock-invariant failures."
    - "Handler extraction map reflects verified delegation for import/export critical handlers."
  artifacts:
    - path: "core/services/inventory_tx_service.py"
      provides: "Import/export transactional business logic boundary"
    - path: "tests/services/test_inventory_tx_service.py"
      provides: "Inventory transaction domain test coverage"
    - path: ".planning/phases/12-service-boundary-extraction/12-handler-extraction-map.md"
      provides: "Finalized delegation traceability for Phase 12 scope"
  key_links:
    - from: "app.py"
      to: "core/services/inventory_tx_service.py"
      via: "api_create_import/api_create_export delegation"
      pattern: "inventory_tx_service\."
    - from: "tests/services/test_inventory_route_delegation.py"
      to: "app.py"
      via: "route delegation assertions"
      pattern: "api_create_import|api_create_export"
    - from: "scripts/phase11_guardrail_check.py"
      to: "tests/contracts/test_contract_routes.py"
      via: "contract parity enforcement"
      pattern: "pytest"
---

<objective>
Complete high-risk import/export extraction into service boundaries and close Phase 12 with service tests plus contract parity verification.

Purpose: Finish REFAC-05 for inventory transaction hotspots and finalize REFAC-06 traceability while proving TEST-02 coverage and parity stability.
Output: Delegated import/export routes, inventory service tests, finalized extraction map statuses, and passing parity gate.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-service-boundary-extraction/12-CONTEXT.md
@.planning/phases/12-service-boundary-extraction/12-RESEARCH.md
@.planning/phases/12-service-boundary-extraction/12-VALIDATION.md
@.planning/phases/12-service-boundary-extraction/12-handler-extraction-map.md
@app.py
@core/database.py
@core/services/inventory_tx_service.py
@scripts/phase11_guardrail_check.py
</context>

<threat_model>

## Threat Model

- **T-12-07 (High):** Inventory mutation logic refactor introduces stock inconsistency or transaction rollback bugs.
  - Mitigation: Centralize transaction logic in `inventory_tx_service.py` and add unit tests for stock failure/rollback scenarios.
- **T-12-08 (Medium):** Import/export routes appear delegated but still contain hidden business branches.
  - Mitigation: Add route delegation tests asserting service-call paths for create-import/create-export handlers.
- **T-12-09 (Medium):** Phase closes without parity verification against baseline contracts.
  - Mitigation: Run `python scripts/phase11_guardrail_check.py` as mandatory final verify step.
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract import/export transactional logic into inventory_tx_service</name>
  <read_first>
    - app.py
    - core/services/inventory_tx_service.py
    - core/database.py
    - .planning/phases/12-service-boundary-extraction/12-handler-extraction-map.md
    - .planning/phases/12-service-boundary-extraction/12-CONTEXT.md
  </read_first>
  <files>core/services/inventory_tx_service.py, app.py</files>
  <behavior>
    - `api_create_import` and `api_create_export` routes delegate business transactions to inventory_tx_service.
    - Service functions enforce stock invariants and explicit transaction rollback on failure.
    - Routes keep endpoint and response contracts while becoming thin wrappers.
  </behavior>
  <action>Implement `inventory_tx_service.py` functions `create_import_transaction(db_conn, user_id, payload)`, `get_import_transaction_details(db_conn, import_id)`, `create_export_transaction(db_conn, user_id, payload, automation_engine)`, and `get_export_transaction_details(db_conn, export_id)`. Move quantity/price calculations, stock checks, product upsert logic, and rollback behavior from `api_create_import`, `api_create_export`, `api_get_import_details`, and `api_get_export_details` into these functions. Update corresponding routes in `app.py` to parse HTTP input, call service functions, and map known service exceptions to 400/404 while preserving existing success payload keys (`success`, `message`, `id`, `transaction`, `details`).</action>
  <acceptance_criteria>
    - `app.py` import/export create/detail routes call `inventory_tx_service.` functions.
    - `inventory_tx_service.py` contains all four required function definitions.
    - Route success payload keys remain unchanged (`success` plus expected result keys).
  </acceptance_criteria>
  <verify>
    <automated>grep -n "inventory_tx_service\.\|def create_import_transaction\|def create_export_transaction" app.py core/services/inventory_tx_service.py</automated>
  </verify>
  <done>Import/export route business logic is delegated to a transactional service boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add inventory service tests and route delegation checks</name>
  <read_first>
    - core/services/inventory_tx_service.py
    - app.py
    - tests/services/conftest.py
    - tests/contracts/test_contract_smoke.py
  </read_first>
  <files>tests/services/test_inventory_tx_service.py, tests/services/test_inventory_route_delegation.py, .planning/phases/12-service-boundary-extraction/12-handler-extraction-map.md</files>
  <behavior>
    - Inventory service tests cover normal transaction flow and insufficient-stock failure paths.
    - Route delegation tests confirm critical import/export handlers call service-layer functions.
    - Extraction map statuses are updated to `verified` for import/export rows with test references.
  </behavior>
  <action>Create `tests/services/test_inventory_tx_service.py` with unit tests for import total calculation, export stock decrement, and insufficient-stock exception rollback branch. Create `tests/services/test_inventory_route_delegation.py` to assert `api_create_import` and `api_create_export` route functions invoke `inventory_tx_service` functions (use monkeypatch/mocks). Update `12-handler-extraction-map.md` import/export rows to `verified` and link both test files in `Test Reference` column.</action>
  <acceptance_criteria>
    - `test_inventory_tx_service.py` includes at least one insufficient-stock failure test.
    - `test_inventory_route_delegation.py` includes assertions for both `api_create_import` and `api_create_export` delegation.
    - Extraction map import/export rows are `verified` with references to both new test files.
    - `python -m pytest tests/services/test_inventory_tx_service.py tests/services/test_inventory_route_delegation.py -q` exits with code 0.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/services/test_inventory_tx_service.py tests/services/test_inventory_route_delegation.py -q</automated>
  </verify>
  <done>Inventory service and delegation tests pass and map traceability is finalized for import/export handlers.</done>
</task>

<task type="auto">
  <name>Task 3: Run full parity gate before Phase 12 completion</name>
  <read_first>
    - scripts/phase11_guardrail_check.py
    - tests/contracts/test_contract_routes.py
    - tests/contracts/test_contract_smoke.py
    - .planning/phases/12-service-boundary-extraction/12-handler-extraction-map.md
  </read_first>
  <files>.planning/phases/12-service-boundary-extraction/12-handler-extraction-map.md</files>
  <action>Run `python scripts/phase11_guardrail_check.py` after inventory extraction and service tests pass. If any route contract check fails, fix delegation regressions before marking rows as complete. Update extraction map `Delegation Status` to `verified` only for handlers that pass both service tests and guardrail parity command in this task.</action>
  <acceptance_criteria>
    - Guardrail command exits 0.
    - Extraction map contains `verified` status for workflow, AI, import, and export high-risk rows.
    - No row is marked `verified` without a corresponding test reference.
  </acceptance_criteria>
  <verify>
    <automated>python scripts/phase11_guardrail_check.py</automated>
  </verify>
  <done>Phase 12 extraction is parity-verified against baseline contracts before closeout.</done>
</task>

</tasks>

<verification>
- Run inventory service and route-delegation tests.
- Run parity guardrail command.
- Confirm extraction map status transitions and references are complete for high-risk handlers.
</verification>

<success_criteria>

- REFAC-05 complete for Phase 12 high-risk business logic domains.
- REFAC-06 extraction map provides verified migration traceability.
- TEST-02 service-layer unit coverage is present and executable.
</success_criteria>

<output>
After completion, create `.planning/phases/12-service-boundary-extraction/12-03-SUMMARY.md`.
</output>
