---
phase: 12-service-boundary-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/12-service-boundary-extraction/12-handler-extraction-map.md
  - core/services/service_errors.py
  - core/services/workflow_service.py
  - core/services/ai_chat_service.py
  - core/services/inventory_tx_service.py
  - tests/services/conftest.py
  - tests/services/test_extraction_contracts.py
autonomous: true
requirements:
  - REFAC-06
  - TEST-02
must_haves:
  truths:
    - "A handler-to-service extraction map exists and names concrete migration targets for high-risk routes."
    - "Service-layer contracts are defined with Flask-independent signatures before route delegation begins."
    - "A dedicated service-test scaffold exists and is runnable with pytest."
  artifacts:
    - path: ".planning/phases/12-service-boundary-extraction/12-handler-extraction-map.md"
      provides: "Traceable migration ledger for REFAC-06"
    - path: "core/services/workflow_service.py"
      provides: "Workflow service contract surface"
    - path: "core/services/ai_chat_service.py"
      provides: "AI chat service contract surface"
    - path: "core/services/inventory_tx_service.py"
      provides: "Import/export transaction service contract surface"
    - path: "tests/services/test_extraction_contracts.py"
      provides: "Automated contract checks for service extraction boundaries"
  key_links:
    - from: "tests/services/test_extraction_contracts.py"
      to: "core/services/workflow_service.py"
      via: "signature and import-boundary assertions"
      pattern: "import core.services.workflow_service"
    - from: ".planning/phases/12-service-boundary-extraction/12-handler-extraction-map.md"
      to: "app.py"
      via: "handler/function mapping"
      pattern: "run_workflow|ai_chat|api_create_import|api_create_export"
---

<objective>
Establish the extraction contract layer and migration ledger so subsequent plans can safely move high-risk business logic out of route bodies.

Purpose: Create an implementation baseline that enforces Flask-independent service boundaries and handler traceability before any delegation changes.
Output: Handler extraction map, service contract modules, and service-level pytest scaffold.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12-service-boundary-extraction/12-CONTEXT.md
@.planning/phases/12-service-boundary-extraction/12-RESEARCH.md
@.planning/phases/12-service-boundary-extraction/12-VALIDATION.md
@app.py
@core/database.py
@core/workflow_engine.py
@core/agent_middleware.py
@tests/contracts/test_contract_routes.py
</context>

<threat_model>

## Threat Model

- **T-12-01 (High):** Extraction starts without a canonical handler map, causing orphaned route logic and unverifiable migration status.
  - Mitigation: Create and maintain `12-handler-extraction-map.md` with explicit endpoint-to-service rows and status fields.
- **T-12-02 (Medium):** Service signatures accidentally bind to Flask globals (`request`, `current_user`), reducing testability and violating REFAC-05.
  - Mitigation: Define plain Python service contracts and enforce with extraction-contract tests.
- **T-12-03 (Medium):** Phase introduces service files without runnable unit-test entrypoint, delaying TEST-02 readiness.
  - Mitigation: Add `tests/services` scaffold and ensure pytest quick command passes.
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create handler extraction map with concrete migration targets</name>
  <read_first>
    - app.py
    - .planning/phases/12-service-boundary-extraction/12-CONTEXT.md
    - .planning/phases/12-service-boundary-extraction/12-RESEARCH.md
    - .planning/ROADMAP.md
  </read_first>
  <files>.planning/phases/12-service-boundary-extraction/12-handler-extraction-map.md</files>
  <behavior>
    - Map captures critical high-risk handlers from workflow, AI, and import/export domains.
    - Each row includes source handler, target service function, current status, and test reference.
  </behavior>
  <action>Create `12-handler-extraction-map.md` with a markdown table containing columns `Endpoint`, `Method`, `Current Handler`, `Target Service Function`, `Delegation Status`, and `Test Reference`. Seed rows for at least these handlers: `run_workflow` (`/api/workflow/execute`), `get_user_workflows` + `save_workflow`, `ai_chat` + `ai_job_status` + `get_chat_history`, `api_create_import`, and `api_create_export`. Set initial statuses to `pending` and add planned test refs under `tests/services/` for each row.</action>
  <acceptance_criteria>
    - `12-handler-extraction-map.md` exists and contains header `| Endpoint | Method | Current Handler | Target Service Function | Delegation Status | Test Reference |`.
    - File contains `run_workflow`, `ai_chat`, `api_create_import`, and `api_create_export` rows.
    - Every seeded row has `Delegation Status` set to `pending`.
  </acceptance_criteria>
  <verify>
    <automated>grep -n "run_workflow\|ai_chat\|api_create_import\|api_create_export\|Delegation Status" .planning/phases/12-service-boundary-extraction/12-handler-extraction-map.md</automated>
  </verify>
  <done>Extraction map exists with concrete high-risk migration targets and pending status baseline.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define service contracts and Flask-independent test scaffold</name>
  <read_first>
    - core/database.py
    - core/workflow_engine.py
    - core/agent_middleware.py
    - tests/contracts/test_contract_routes.py
    - .planning/phases/12-service-boundary-extraction/12-VALIDATION.md
  </read_first>
  <files>core/services/service_errors.py, core/services/workflow_service.py, core/services/ai_chat_service.py, core/services/inventory_tx_service.py, tests/services/conftest.py, tests/services/test_extraction_contracts.py</files>
  <behavior>
    - Service modules expose explicit function signatures for planned route delegation.
    - Contract tests enforce that service modules do not import Flask request/session globals.
    - Pytest service scaffold runs independently of Flask route client fixtures.
  </behavior>
  <action>Create `core/services/service_errors.py` with exceptions `ServiceValidationError`, `ServiceAuthorizationError`, and `ServiceInvariantError`. Create contract-first modules `workflow_service.py`, `ai_chat_service.py`, and `inventory_tx_service.py` with concrete function signatures: `execute_user_workflow(workflow_data, google_token_raw)`, `list_workflows_for_user(db_conn, user_id)`, `submit_chat_message(user_id, message)`, `get_chat_history_rows(db_conn, user_id, limit=50)`, `create_import_transaction(db_conn, user_id, payload)`, and `create_export_transaction(db_conn, user_id, payload)`. Add `tests/services/conftest.py` fixtures for plain dict payloads and DB stubs. Add `tests/services/test_extraction_contracts.py` that imports all three service modules, asserts required callables exist, and asserts module source does not contain `from flask import`, `request`, or `current_user`.</action>
  <acceptance_criteria>
    - `core/services/service_errors.py` defines all three exception class names.
    - `workflow_service.py`, `ai_chat_service.py`, and `inventory_tx_service.py` each contain at least one required function signature from the action spec.
    - `tests/services/test_extraction_contracts.py` contains assertions checking absence of Flask globals in service modules.
    - `python -m pytest tests/services/test_extraction_contracts.py -q` exits with code 0.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/services/test_extraction_contracts.py -q</automated>
  </verify>
  <done>Service extraction contracts and service-test scaffold exist and are executable.</done>
</task>

</tasks>

<verification>
- Confirm extraction map and service contract modules exist.
- Run service extraction contract tests.
- Confirm no Flask request/session imports inside new service modules.
</verification>

<success_criteria>

- REFAC-06 foundation is complete with explicit handler migration mapping.
- TEST-02 Wave 0 setup exists and runs via pytest for service contracts.
- Phase is ready for route delegation extraction in subsequent plans.
</success_criteria>

<output>
After completion, create `.planning/phases/12-service-boundary-extraction/12-01-SUMMARY.md`.
</output>
