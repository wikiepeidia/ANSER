---
phase: 14-backend-branch-integration-checkpoint
plan: 01
type: execute
wave: 1
depends_on:
  - 13-01
files_modified:
  - requirements-dev.txt
  - .coveragerc
  - scripts/phase14_backend_coverage_gate.py
  - .planning/phases/14-backend-branch-integration-checkpoint/14-backend-coverage-report.md
autonomous: true
requirements:
  - TEST-04
must_haves:
  truths:
    - "Backend integration gate produces an explicit coverage percentage on every run."
    - "Coverage threshold is enforceable and fails when below target."
    - "Coverage evidence is persisted as a phase-local artifact for auditability."
  artifacts:
    - path: "scripts/phase14_backend_coverage_gate.py"
      provides: "single-command backend coverage gate"
    - path: ".coveragerc"
      provides: "backend-focused coverage source/include configuration"
    - path: "requirements-dev.txt"
      provides: "test tooling dependencies for coverage execution"
    - path: ".planning/phases/14-backend-branch-integration-checkpoint/14-backend-coverage-report.md"
      provides: "human-readable coverage gate evidence"
  key_links:
    - from: "scripts/phase14_backend_coverage_gate.py"
      to: "pytest-cov"
      via: "subprocess pytest invocation with --cov and --cov-fail-under"
      pattern: "--cov|--cov-fail-under"
    - from: "scripts/phase14_backend_coverage_gate.py"
      to: "14-backend-coverage-report.md"
      via: "report generation"
      pattern: "coverage report"
---

<objective>
Create a repeatable backend coverage gate with an enforceable threshold and auditable report output.

Purpose: Satisfy TEST-04 by making backend refactor quality measurable and fail-fast before merge preparation.
Output: Coverage configuration, gate script, and generated Phase 14 coverage report.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-backend-branch-integration-checkpoint/14-CONTEXT.md
@pytest.ini
@scripts/phase11_guardrail_check.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add backend coverage tooling baseline</name>
  <files>requirements-dev.txt, .coveragerc</files>
  <behavior>
    - Coverage dependencies are explicitly declared for local and CI execution.
    - Coverage configuration targets backend refactor surfaces and excludes irrelevant paths.
  </behavior>
  <action>Create requirements-dev.txt with pytest-cov and supporting pytest tooling. Add .coveragerc scoping backend modules (core, routes, app composition paths) and excluding test files and non-runtime assets.</action>
  <verify>
    <automated>python -m pip install -r requirements-dev.txt && python -m pytest --help | grep -E "--cov|--cov-fail-under"</automated>
  </verify>
  <done>Coverage tooling is installable and pytest exposes coverage options.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement coverage gate runner with threshold support</name>
  <files>scripts/phase14_backend_coverage_gate.py</files>
  <behavior>
    - Gate command runs backend suites with coverage threshold enforcement.
    - Exit code is non-zero when threshold is not met.
  </behavior>
  <action>Create scripts/phase14_backend_coverage_gate.py to run targeted backend tests (contracts + services) with --cov and --cov-fail-under. Expose threshold as CLI arg with default baseline value, and emit concise pass/fail summary.</action>
  <verify>
    <automated>python scripts/phase14_backend_coverage_gate.py --help</automated>
  </verify>
  <done>Coverage gate runner is executable and configurable via command arguments.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Generate and persist Phase 14 coverage evidence</name>
  <files>.planning/phases/14-backend-branch-integration-checkpoint/14-backend-coverage-report.md</files>
  <behavior>
    - Coverage run produces a report artifact with threshold, actual result, and pass/fail decision.
    - Report can be referenced by downstream integration checkpoint documentation.
  </behavior>
  <action>Execute coverage gate script and write report markdown in phase directory containing command, threshold, measured coverage, and gate outcome. Include timestamp and quick guidance for raising threshold over time.</action>
  <verify>
    <automated>python scripts/phase14_backend_coverage_gate.py --threshold 20 && test -f .planning/phases/14-backend-branch-integration-checkpoint/14-backend-coverage-report.md</automated>
  </verify>
  <done>Coverage report exists and records a deterministic pass/fail gate decision.</done>
</task>

</tasks>

<verification>
- Coverage gate script runs successfully and enforces threshold behavior.
- Report artifact exists and captures gate details for audit.
</verification>

<success_criteria>

- TEST-04 marked complete with enforceable threshold and reproducible evidence.
</success_criteria>

<output>
After completion, create .planning/phases/14-backend-branch-integration-checkpoint/14-01-SUMMARY.md
</output>
