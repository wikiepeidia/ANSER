---
phase: 29-production-security-defaults
plan: 01
type: execute
requirements:
  - SEC-01
---

# Phase 29 Plan 01: Production Security Defaults

## Tasks

1. Add environment-aware security helpers for local vs production-like runtime.
2. Configure `SESSION_COOKIE_SECURE`, `RATELIMIT_ENABLED`, HTTPS redirects, HSTS, and `MAX_CONTENT_LENGTH` from secure defaults.
3. Preserve local/test ergonomics and existing test-mode limiter behavior.
4. Verify login limiter remains wired to `/auth/signin`.

## Verification

- `pytest -q`

