---
phase: 26-automation-engine-schema-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - core/automation_engine.py
  - tests/test_automation_smoke.py
autonomous: true
requirements:
  - AUTO-01
  - AUTO-02

must_haves:
  truths:
    - "core/automation_engine.py contains no reference to import_price column"
    - "core/automation_engine.py contains no reference to the suppliers table"
    - "core/automation_engine.py contains no reference to supplier_id column in INSERT statements"
    - "execute_import_automation runs against an in-memory SQLite database without raising an exception"
    - "execute_scheduled_import runs against an in-memory SQLite database without raising an exception"
  artifacts:
    - path: "core/automation_engine.py"
      provides: "Fixed automation engine with correct schema references"
      contains: "supplier_name"
    - path: "tests/test_automation_smoke.py"
      provides: "Smoke test exercising both automation methods against in-memory SQLite"
      exports: ["test_execute_import_automation", "test_execute_scheduled_import"]
  key_links:
    - from: "tests/test_automation_smoke.py"
      to: "core/automation_engine.py"
      via: "AutomationEngine(db) instantiation"
      pattern: "AutomationEngine"
    - from: "core/automation_engine.py"
      to: "import_transactions"
      via: "INSERT with supplier_name"
      pattern: "supplier_name"
---

<objective>
Fix all schema mismatches in core/automation_engine.py so it only references tables and columns
that exist in the actual SQLite database, then prove the fix works by running a smoke test
against an in-memory database.

Purpose: The automation engine currently references import_price (does not exist in products),
the suppliers table (does not exist), and supplier_id (does not exist in import_transactions).
These bugs cause runtime errors whenever a low-stock or scheduled automation fires.

Output: A corrected automation_engine.py with no phantom column/table references, plus a
passing smoke test at tests/test_automation_smoke.py.
</objective>

<execution_context>
@C:/Users/wikiepeidia/OneDrive - caugiay.edu.vn/bài tập/usth/GEN14/GROUP project/Group-project-AI-ML/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/wikiepeidia/OneDrive - caugiay.edu.vn/bài tập/usth/GEN14/GROUP project/Group-project-AI-ML/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-automation-engine-schema-fix/26-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix schema mismatches in core/automation_engine.py (AUTO-01)</name>
  <files>core/automation_engine.py</files>

  <read_first>
    - core/automation_engine.py — read in full before making any edits; understand both
      execute_import_automation (lines 135-173) and execute_scheduled_import (lines 175-226)
    - core/db/connection.py — read init_database() to confirm: products columns are
      id, code, name, category, unit, price, stock_quantity, description, created_by,
      created_at, updated_at, image_url; import_transactions columns are
      id, code, supplier_name, total_amount, notes, status, created_by, created_at;
      import_details columns are id, import_id, product_id, quantity, unit_price, total_price
  </read_first>

  <action>
    Apply the following targeted edits to core/automation_engine.py. Make no other changes.

    In execute_import_automation (around lines 135-173):

    1. Line 145: Change "SELECT name, import_price FROM products WHERE id = ?" to
       "SELECT name, price FROM products WHERE id = ?"

    2. Line 147: Change "prod['import_price']" to "prod['price']" in both the conditional
       check and the value read. The full expression becomes:
       unit_price = prod['price'] if prod and prod['price'] else 0

    3. Lines 151-153: Remove the three lines that query suppliers and assign supplier_id:
         c.execute("SELECT id FROM suppliers LIMIT 1")
         supplier = c.fetchone()
         supplier_id = supplier['id'] if supplier else 1
       Replace them with a single assignment:
         supplier_name = 'Automated Import'

    4. Lines 155-158: In the INSERT INTO import_transactions statement, change the column
       list from (code, supplier_id, total_amount, status, notes, created_by) to
       (code, supplier_name, total_amount, status, notes, created_by) and replace the
       supplier_id positional argument with supplier_name.

    In execute_scheduled_import (around lines 175-226):

    5. Line 185: Change "SELECT id, stock_quantity, import_price FROM products WHERE
       stock_quantity < ?" to "SELECT id, stock_quantity, price FROM products WHERE
       stock_quantity < ?"

    6. Lines 194-196: Remove the three lines that query suppliers and assign supplier_id:
         c.execute("SELECT id FROM suppliers LIMIT 1")
         supplier = c.fetchone()
         supplier_id = supplier['id'] if supplier else 1
       Replace them with a single assignment:
         supplier_name = 'Automated Import'

    7. Line 203: Change "prod['import_price'] if prod['import_price'] else 0" to
       "prod['price'] if prod['price'] else 0"

    8. Lines 207-210: In the INSERT INTO import_transactions statement, change the column
       list from (code, supplier_id, total_amount, status, notes, created_by) to
       (code, supplier_name, total_amount, status, notes, created_by) and replace the
       supplier_id positional argument with supplier_name.

    Do not alter check_low_stock, check_scheduled_automations, the INSERT INTO import_details
    statements, logging calls, exception handling, or any other code. Column and table names
    in import_details are already correct — leave them untouched.
  </action>

  <acceptance_criteria>
    - grep -n "import_price" core/automation_engine.py returns no matches (exit code 1, empty output)
    - grep -n "suppliers" core/automation_engine.py returns no matches
    - grep -n "supplier_id" core/automation_engine.py returns no matches
    - grep -n "supplier_name" core/automation_engine.py returns at least 4 matches
      (two assignments + two INSERT column references)
    - python -c "from core.automation_engine import AutomationEngine" exits 0 with no output
  </acceptance_criteria>

  <verify>
    <automated>
      grep -n "import_price" core/automation_engine.py; echo "import_price_count=$?"
      grep -n "suppliers" core/automation_engine.py; echo "suppliers_count=$?"
      grep -n "supplier_id" core/automation_engine.py; echo "supplier_id_count=$?"
    </automated>
  </verify>

  <done>
    All three grep commands return no matches. The file imports cleanly. No other lines in
    the file were modified beyond the eight targeted edits above.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write and run smoke test for both automation methods (AUTO-02)</name>
  <files>tests/test_automation_smoke.py</files>

  <read_first>
    - core/automation_engine.py — read the corrected file from Task 1 to understand method
      signatures: execute_import_automation(self, auto_id, config, product_id) and
      execute_scheduled_import(self, auto_id, config)
    - core/db/connection.py — read the Database class constructor signature and init_database()
      to understand how to create an in-memory instance; confirm the se_automations table
      schema: id, name, type, config, enabled, last_run, created_at
  </read_first>

  <action>
    Create tests/test_automation_smoke.py with two test functions using pytest. The file
    must not import Flask or the main app — only core modules.

    Module-level setup:
    - Import Database from core.db.connection
    - Import AutomationEngine from core.automation_engine
    - Import json

    Write a module-level fixture function or helper named make_db() that:
    - Instantiates Database with sqlite_path=':memory:' and use_postgres=False
    - Calls db.init_database() to create all tables
    - Returns the db instance

    Test function test_execute_import_automation():
    - Call make_db() to get a fresh in-memory db
    - Instantiate AutomationEngine(db)
    - Insert one row into products: use an INSERT with columns
      (code, name, category, unit, price, stock_quantity, created_by) and values
      ('P-TEST', 'Test Product', 'test', 'pcs', 100.0, 5, 1)
    - Retrieve the inserted product id via lastrowid or a SELECT
    - Insert one row into se_automations with columns
      (name, type, config, enabled) and values
      ('Test Auto', 'low_stock', json.dumps({'threshold': 10, 'reorder_quantity': 20}), 1)
    - Retrieve the automation id
    - Call engine.execute_import_automation(auto_id, {'threshold': 10, 'reorder_quantity': 20}, product_id)
    - Assert no exception was raised (the call itself proves this if no try/except swallows it)
    - Query import_transactions WHERE code LIKE 'IMP-AUTO-%' and assert rowcount >= 1
    - Close the db connection

    Test function test_execute_scheduled_import():
    - Call make_db() to get a fresh in-memory db
    - Instantiate AutomationEngine(db)
    - Insert one row into products with stock_quantity=5 (below the threshold=20 used by
      execute_scheduled_import) using the same column set as above
    - Insert one row into se_automations (same as above)
    - Retrieve the automation id
    - Call engine.execute_scheduled_import(auto_id, {})
    - Assert no exception was raised
    - Query import_transactions WHERE code LIKE 'IMP-SCH-%' and assert rowcount >= 1
    - Close the db connection

    Use pytest — no unittest.TestCase classes. Both functions must be named
    test_execute_import_automation and test_execute_scheduled_import exactly.

    The Database constructor parameter names: confirm by reading core/db/connection.py before
    writing the call. Use whatever parameter names the actual class accepts for the path and
    postgres flag — do not guess; read first.
  </action>

  <acceptance_criteria>
    - python -m pytest tests/test_automation_smoke.py -v exits 0
    - Both test_execute_import_automation and test_execute_scheduled_import are collected and pass
    - No import errors or OperationalError (no such column / no such table) appear in output
    - The test file does not import app, Flask, or any route module
  </acceptance_criteria>

  <verify>
    <automated>python -m pytest tests/test_automation_smoke.py -v</automated>
  </verify>

  <done>
    pytest reports 2 passed, 0 failed, 0 errors. The output shows both test names collected
    and marked PASSED. No OperationalError or AttributeError appears anywhere in the run.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| automation engine -> SQLite | SQL strings constructed from config dict values; config comes from se_automations.config (DB-stored, not user-facing in this phase) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | Tampering | execute_import_automation config dict | accept | config is read from the internal se_automations table, not from HTTP input; no user-supplied data crosses this boundary in this phase |
| T-26-02 | Information Disclosure | smoke test output | accept | test runs against :memory: only; no production credentials or data touched |
| T-26-SC | Tampering | pip/package installs | accept | no new packages installed in this phase; pytest is already a dev dependency |
</threat_model>

<verification>
After both tasks complete, run in sequence:

1. grep -n "import_price" core/automation_engine.py  — must return no output
2. grep -n "suppliers" core/automation_engine.py     — must return no output
3. grep -n "supplier_id" core/automation_engine.py   — must return no output
4. python -m pytest tests/test_automation_smoke.py -v — must report 2 passed

All four checks must pass before the phase is considered complete.
</verification>

<success_criteria>
- AUTO-01: automation_engine.py references only price (not import_price), supplier_name (not
  supplier_id), and has no query against the suppliers table
- AUTO-02: pytest tests/test_automation_smoke.py reports 2 passed with no errors or
  OperationalError output
- Phase 26 ROADMAP success criteria satisfied:
  1. No reference to suppliers table or import_price column in automation_engine.py
  2. check_low_stock / execute_import_automation path verified via smoke test
  3. execute_scheduled_import verified via smoke test
</success_criteria>

## Artifacts this phase produces

| Artifact | Type | Purpose |
|----------|------|---------|
| core/automation_engine.py | Modified file | Schema-correct automation engine |
| tests/test_automation_smoke.py | New file | Runnable smoke test for both automation methods |

<output>
Create `.planning/phases/26-automation-engine-schema-fix/26-01-SUMMARY.md` when done.
</output>
