# Quick Task Plan: Continue Slimming routes/main_routes.py

Objective
Continue slimming `routes/main_routes.py` by extracting only the admin and user-management route block that starts at `/api/admin/users` and ends at `/api/manager/users-permissions`, while preserving current route registration and middleware behavior.

Scope
- In scope: the admin/user-management endpoints in that contiguous block.
- Out of scope: any customer or product extraction in this step.
- Reason to stay narrow: `routes/main_routes.py` still has duplicated `/api/customers*` and `/api/products*` endpoint definitions, so that surface is higher risk and should remain untouched here.

Constraints
- Preserve the existing extraction pattern used by `routes/page_routes.py`, `routes/sales_routes.py`, and `routes/workspace_routes.py`.
- The new module must import `app as app_module`, copy shared globals from `app_module`, and expose a `register_*_routes(app)` function.
- Wire the new module from `routes/main_routes.py` using the same composition-root style as the current extracted modules.
- Do not make a git commit as part of this quick task.

## Task 1: Extract the admin/user-management route block into a dedicated module
Files
- `routes/main_routes.py`
- `routes/admin_user_routes.py`

Action
- Create `routes/admin_user_routes.py` with the established extracted-route-module pattern:
  - `import app as app_module`
  - copy initialized globals from `app_module`
  - define `register_admin_user_routes(app)`
- Move only these routes from `routes/main_routes.py` into the new module, preserving behavior and decorators:
  - `/api/admin/users`
  - `/api/admin/activity`
  - `/api/admin/stats`
  - `/api/admin/create-manager`
  - `/api/create-user`
  - `/api/users`
  - `/api/users/<int:user_id>`
  - `/api/users/<int:user_id>/reset-password`
  - `/api/admin/users/<int:user_id>`
  - `/api/admin/users/promote`
  - `/api/admin/users/demote`
  - `/api/manager/users-permissions`
- Update `routes/main_routes.py` to import `register_admin_user_routes` and call it from `register_main_routes(app)` alongside the existing extracted route modules.
- Leave every `/api/customers*` and `/api/products*` definition in `routes/main_routes.py` untouched in this step.

Verify
- Route registration smoke check:
  - `python -c "import app as app_module; app = getattr(app_module, 'app', None) or app_module.create_app(); route_map = {}; [route_map.setdefault(rule.rule, set()).update({m for m in rule.methods if m not in {'HEAD', 'OPTIONS'}}) for rule in app.url_map.iter_rules()]; assert {'GET'} <= route_map['/api/admin/users']; assert {'GET'} <= route_map['/api/manager/users-permissions']; print('admin/user routes registered')"`
- Quick local inspection confirms `routes/main_routes.py` still contains the duplicated customer/product route area after the extracted block.

## Task 2: Add focused route-registration contract coverage for the extracted endpoints
Files
- `tests/contracts/test_contract_routes.py`

Action
- Extend the route-registration contract test file with a focused assertion block for the extracted admin/user-management routes.
- Reuse the existing `_route_method_map(app)` helper and assert the expected methods for the moved endpoints so extraction failures show up as registration regressions, not only import errors.
- Keep this coverage route-focused; do not expand it into deeper response-shape or data-behavior tests.

Verify
- `python -m pytest tests/contracts/test_contract_routes.py -q`

## Task 3: Extend parity coverage for middleware behavior on representative extracted routes
Files
- `tests/parity/test_endpoint_middleware_parity.py`

Action
- Add focused parity coverage for representative extracted endpoints so the route split still preserves current middleware behavior through the app registry.
- At minimum, extend the unauthorized JSON-401 API parity set to include:
  - `/api/admin/users`
  - `/api/manager/users-permissions`
- Keep this parity task middleware-scoped and low-risk: verify registration-level access behavior, not full admin/manager business logic.

Verify
- `python -m pytest tests/parity/test_endpoint_middleware_parity.py -q`

## Done When
- The admin/user-management block has moved into `routes/admin_user_routes.py` and is registered from `routes/main_routes.py` using the existing extraction pattern.
- `routes/main_routes.py` no longer owns the extracted admin/user-management handlers, but still owns the duplicated customer/product surface.
- Focused route-registration coverage exists for the moved endpoints.
- Focused middleware parity coverage exists for representative moved endpoints.
- Final focused validation passes:
  - `python -m pytest tests/contracts/test_contract_routes.py tests/parity/test_endpoint_middleware_parity.py -q`
- No git commit was made.
