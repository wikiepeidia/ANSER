---
type: quick-task-plan
quick_task: 260422-h72
title: Continue slimming routes/main_routes.py admin subscription-management block
status: planned
created: 2026-04-22
commit_required: false
---

# Objective
Continue slimming `routes/main_routes.py` by extracting only the contiguous admin subscription-management block into a dedicated routes module, while preserving route registration and middleware behavior.

# Context
- `.planning/STATE.md`
- `routes/main_routes.py`
- `routes/operations_routes.py`
- `routes/admin_user_routes.py`
- `tests/contracts/test_contract_routes.py`
- `tests/parity/test_endpoint_middleware_parity.py`

# Scope
In scope:
- `GET /api/admin/subscriptions`
- `POST /api/admin/subscription/auto-renew`
- `POST /api/admin/subscription/extend`
- `GET /api/admin/subscription-history`
- `POST /api/admin/extend-subscription`
- `POST /api/admin/check-expired-subscriptions`

Out of scope:
- All duplicated `/api/customers*` routes
- All duplicated `/api/products*` routes
- Any non-subscription route cleanup in `routes/main_routes.py`
- Business-logic changes beyond extraction-safe validation adjustments

# Constraints
- Preserve the existing extraction pattern: dedicated routes module, `import app as app_module`, copy globals from `app_module`, and expose `register_admin_subscription_routes(app)`.
- Follow the same composition-root wiring style already used by `routes/admin_user_routes.py` and `routes/operations_routes.py`.
- Update `routes/main_routes.py` only enough to import and call `register_admin_subscription_routes(app)`.
- Keep the full subscription-management block together in one new module for this step.
- Do not make a git commit for this quick task.

# Tasks

## Task 1: Extract the admin subscription-management block into a dedicated routes module
Files:
- `routes/main_routes.py`
- `routes/admin_subscription_routes.py`

Action:
- Move only the contiguous block that starts at `GET /api/admin/subscriptions` and ends at `POST /api/admin/check-expired-subscriptions` out of `routes/main_routes.py`.
- Preserve current handlers, decorators, helper usage, and response shapes, including the existing CSRF exemptions and both extend-subscription variants.
- Implement the new module with `import app as app_module`, shared-global copying, and `register_admin_subscription_routes(app)`.
- Wire `register_admin_subscription_routes(app)` inside `register_main_routes(app)` alongside the existing extracted route modules.
- Leave every duplicated `/api/customers*` and `/api/products*` route exactly where it is.

Verify:
- Route registration smoke check:
  - `python -c "import app as app_module; app = getattr(app_module, 'app', None) or app_module.create_app(); route_map = {}; [route_map.setdefault(rule.rule, set()).update({m for m in rule.methods if m not in {'HEAD', 'OPTIONS'}}) for rule in app.url_map.iter_rules()]; assert {'GET'} <= route_map['/api/admin/subscriptions']; assert {'POST'} <= route_map['/api/admin/subscription/auto-renew']; assert {'POST'} <= route_map['/api/admin/check-expired-subscriptions']; print('admin subscription routes registered')"`

Done when:
- None of the in-scope route decorators remain in `routes/main_routes.py`.
- `routes/admin_subscription_routes.py` owns the extracted block.
- `register_main_routes(app)` imports and calls `register_admin_subscription_routes(app)`.

## Task 2: Add focused route-registration contract coverage for the moved endpoints
Files:
- `tests/contracts/test_contract_routes.py`

Action:
- Add one focused contract test, reusing `_route_method_map(app)`, that asserts the expected methods for every in-scope subscription-management endpoint remain registered in `app.url_map` after extraction.
- Keep this coverage route-focused only: registration and methods, not response payloads or database behavior.
- Name the test so it can be run directly, for example `test_admin_subscription_routes_remain_registered_after_extraction`.

Verify:
- `python -m pytest tests/contracts/test_contract_routes.py -k admin_subscription_routes -x`

Done when:
- The contract suite has a dedicated route-registration check for the moved subscription routes and it passes.

## Task 3: Add relevant parity coverage for representative extracted subscription routes
Files:
- `tests/parity/test_endpoint_middleware_parity.py`

Action:
- Add focused parity coverage for representative extracted subscription routes so the split preserves current middleware behavior through the app registry.
- Cover at least one unauthenticated JSON-401 path from the moved block, such as `GET /api/admin/subscriptions`.
- Add one representative POST-path middleware check from the moved block that matches the current baseline behavior after extraction, rather than forcing a new behavior profile.
- Keep this step route-focused and narrow; do not broaden into full admin subscription business-logic tests.

Verify:
- `python -m pytest tests/parity/test_endpoint_middleware_parity.py -k subscription -x`
- `python -m pytest tests/contracts/test_contract_routes.py -k admin_subscription_routes -x`

Done when:
- The parity suite has targeted coverage for representative moved subscription routes.
- The targeted parity and contract checks both pass.

# Success criteria
- `routes/main_routes.py` is slimmer by exactly the subscription-management block from `/api/admin/subscriptions` through `/api/admin/check-expired-subscriptions`.
- The new routes module follows the existing extraction pattern used by the neighboring extracted route modules.
- Route registration checks cover all moved subscription endpoints.
- Parity coverage confirms representative middleware behavior for the extracted subscription routes.
- Duplicated `/api/customers*` and `/api/products*` routes remain untouched.
- No git commit is made.
