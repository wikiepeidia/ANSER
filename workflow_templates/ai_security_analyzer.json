{
  "name": "AI Security Log Analyzer",
  "nodes": [
    {
      "parameters": {
        "httpMethod": "POST",
        "path": "analyze-log",
        "options": {}
      },
      "id": "node-webhook-001",
      "name": "Receive Log",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 1,
      "position": [180, 300]
    },
    {
      "parameters": {
        "jsCode": "const log = $input.first().json;\nconst processed = {\n  timestamp:     log.timestamp || new Date().toISOString(),\n  event_type:    log.event_type || log.EventType || 'unknown',\n  source_ip:     log.source_ip || log.SourceIP || null,\n  destination:   log.destination || log.DestHost || null,\n  user:          log.user || log.username || null,\n  failure_count: log.failure_count || null,\n  protocol:      log.protocol || null,\n  message:       log.message || log.raw || JSON.stringify(log)\n};\nconst searchQuery = [processed.event_type, processed.source_ip, processed.message]\n  .filter(Boolean).join(' ').substring(0, 300);\nreturn [{ json: { processed, searchQuery, logJson: JSON.stringify(processed, null, 2) } }];"
      },
      "id": "node-code-001",
      "name": "Preprocess Log",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [400, 300]
    },
    {
      "parameters": {
        "jsCode": "const { searchQuery, logJson } = $input.first().json;\nlet ragContext = '';\ntry {\n  const reqBody = JSON.stringify({ text: searchQuery, n_results: 3 });\n  const data = await new Promise((resolve, reject) => {\n    const req = require('http').request({\n      hostname: 'rag-service', port: 8001, path: '/query', method: 'POST',\n      headers: { 'Content-Type': 'application/json', 'Content-Length': Buffer.byteLength(reqBody) }\n    }, res => {\n      let raw = ''; res.on('data', c => raw += c);\n      res.on('end', () => { try { resolve(JSON.parse(raw)); } catch(e) { resolve({}); } });\n    });\n    req.on('error', reject); req.write(reqBody); req.end();\n  });\n  const docs = data?.documents || [];\n  if (docs.length > 0) {\n    ragContext = '\\n\\nSecurity knowledge base context:\\n' + docs.filter(Boolean).join('\\n---\\n');\n  }\n} catch (_) {}\nreturn [{ json: { ragContext, logJson } }];"
      },
      "id": "node-code-rag",
      "name": "Query RAG Context",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [620, 300]
    },
    {
      "parameters": {
        "jsCode": "const { ragContext = '', logJson } = $input.first().json;\nconst systemPrompt = `You are an expert cybersecurity SOC analyst.\nAnalyze the log entry and respond ONLY with valid JSON:\n{\n  \"threat_detected\": <bool>,\n  \"severity\": \"<none|low|medium|high|critical>\",\n  \"confidence\": <0-100>,\n  \"attack_type\": \"<MITRE tactic or null>\",\n  \"mitre_technique\": \"<T1xxx or null>\",\n  \"affected_assets\": [\"<IPs or users>\"],\n  \"reason\": \"<one sentence>\",\n  \"recommended_action\": \"<block_ip|disable_account|escalate|monitor|none>\"\n}\nReturn ONLY the JSON object. No markdown.`;\nconst llmBody = JSON.stringify({\n  model: 'Qwen/Qwen2.5-14B-Instruct-AWQ',\n  messages: [\n    { role: 'system', content: systemPrompt },\n    { role: 'user', content: 'Analyze this log:' + ragContext + '\\n\\n' + logJson }\n  ],\n  max_tokens: 512,\n  temperature: 0.05,\n  response_format: { type: 'json_object' }\n});\nreturn [{ json: { llmBody, logJson } }];"
      },
      "id": "node-code-002",
      "name": "Build LLM Prompt",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [840, 300]
    },
    {
      "parameters": {
        "jsCode": "const { llmBody, logJson } = $input.first().json;\n// UPDATE hostname to ngrok host after running Colab (remove port/use 443 for https)\nconst llmResponse = await new Promise((resolve, reject) => {\n  const req = require('http').request({\n    hostname: 'host.docker.internal', port: 8080,\n    path: '/v1/chat/completions', method: 'POST',\n    headers: { 'Content-Type': 'application/json', 'Content-Length': Buffer.byteLength(llmBody) }\n  }, res => {\n    let raw = ''; res.on('data', c => raw += c);\n    res.on('end', () => { try { resolve(JSON.parse(raw)); } catch(e) { resolve({}); } });\n  });\n  req.on('error', reject); req.write(llmBody); req.end();\n});\nreturn [{ json: { llmResponse, logJson } }];"
      },
      "id": "node-code-003",
      "name": "Call LLM (vLLM)",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [1060, 300]
    },
    {
      "parameters": {
        "jsCode": "const { llmResponse, logJson } = $input.first().json;\nlet analysis;\ntry {\n  const content = llmResponse.choices[0].message.content;\n  analysis = typeof content === 'string' ? JSON.parse(content) : content;\n} catch (e) {\n  analysis = {\n    threat_detected: false, severity: 'none', confidence: 0,\n    attack_type: null, mitre_technique: null, affected_assets: [],\n    reason: 'Parse error: ' + e.message, recommended_action: 'monitor'\n  };\n}\nreturn [{ json: { ...analysis, logJson, analyzed_at: new Date().toISOString() } }];"
      },
      "id": "node-code-004",
      "name": "Parse LLM Response",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [1280, 300]
    },
    {
      "parameters": {
        "conditions": {
          "boolean": [
            {
              "value1": "={{ $json.threat_detected }}",
              "value2": true
            }
          ]
        }
      },
      "id": "node-if-001",
      "name": "Threat Detected?",
      "type": "n8n-nodes-base.if",
      "typeVersion": 1,
      "position": [1500, 300]
    },
    {
      "parameters": {
        "jsCode": "const d = $input.first().json;\nconst ALERT_URL = 'https://webhook.site/REPLACE-WITH-YOUR-ID';\ntry {\n  const alertData = JSON.stringify({\n    alert_type: 'SECURITY_THREAT',\n    severity:   d.severity,\n    confidence: d.confidence + '%',\n    attack:     d.attack_type,\n    mitre:      d.mitre_technique,\n    action:     d.recommended_action,\n    reason:     d.reason,\n    assets:     d.affected_assets,\n    timestamp:  d.analyzed_at\n  });\n  const urlObj = new URL(ALERT_URL);\n  await new Promise((resolve, reject) => {\n    const req = require('https').request({\n      hostname: urlObj.hostname, port: 443,\n      path: urlObj.pathname, method: 'POST',\n      headers: { 'Content-Type': 'application/json', 'Content-Length': Buffer.byteLength(alertData) }\n    }, res => { res.on('data', () => {}); res.on('end', resolve); });\n    req.on('error', reject); req.write(alertData); req.end();\n  });\n} catch (_) {}\nreturn [{ json: { status: 'alerted', ...d } }];"
      },
      "id": "node-code-005",
      "name": "Mock Alert",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [1720, 180]
    },
    {
      "parameters": {
        "jsCode": "const d = $input.first().json;\nconsole.log('[CLEAN]', d.analyzed_at, '| confidence:', d.confidence, '| reason:', d.reason);\nreturn [{ json: { status: 'clean', ...d } }];"
      },
      "id": "node-code-006",
      "name": "Log Clean Event",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [1720, 420]
    }
  ],
  "connections": {
    "Receive Log": {
      "main": [[{ "node": "Preprocess Log",    "type": "main", "index": 0 }]]
    },
    "Preprocess Log": {
      "main": [[{ "node": "Query RAG Context", "type": "main", "index": 0 }]]
    },
    "Query RAG Context": {
      "main": [[{ "node": "Build LLM Prompt",  "type": "main", "index": 0 }]]
    },
    "Build LLM Prompt": {
      "main": [[{ "node": "Call LLM (vLLM)",   "type": "main", "index": 0 }]]
    },
    "Call LLM (vLLM)": {
      "main": [[{ "node": "Parse LLM Response","type": "main", "index": 0 }]]
    },
    "Parse LLM Response": {
      "main": [[{ "node": "Threat Detected?",  "type": "main", "index": 0 }]]
    },
    "Threat Detected?": {
      "main": [
        [{ "node": "Mock Alert",      "type": "main", "index": 0 }],
        [{ "node": "Log Clean Event", "type": "main", "index": 0 }]
      ]
    }
  },
  "active": false,
  "settings": { "executionOrder": "v1" }
}
